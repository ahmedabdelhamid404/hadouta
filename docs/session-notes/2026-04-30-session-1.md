# Session 2026-04-30 — #1 (Project Bootstrap)

**Duration**: ~7 hours (long session — included full brainstorming + design doc + bootstrap)
**Sprint**: Sprint 0 — Project Bootstrap
**Manager**: Claude (Opus 4.7)
**Participant**: Ahmed Abdelhameed

---

## What we did

### Brainstorming (first ~4 hours)
- Walked through 14 question-driven decisions to crystallize the Hadouta concept
- Validated greenfield Egyptian market (research fork: Qessati is Saudi-only, no Egyptian player)
- Validated unit economics (research fork: per-book cost ~70 EGP at scale, healthy margin at 250-300 EGP retail)
- Re-evaluated tech stack with "neglect prior knowledge" directive (research fork): switched from .NET + Angular to Node + Hono + Next.js + shadcn
- Re-evaluated Supabase concerns: switched to Neon + Better-Auth + Cloudflare R2

### Design doc
- Written at `docs/design/2026-04-30-hadouta-design.md`
- 18 sections: executive summary → roadmap → cost model → validation plan
- ~4500 words

### Project bootstrap (this session, after plan-mode approval)
- Installed `uv` runtime
- Reorganized docs folder (moved design doc out of awkward superpowers/specs path)
- Created shared docs structure: `docs/sprints/`, `docs/decisions/`, `docs/session-notes/`, `docs/agents-playbook.md`, `docs/skills-roadmap.md`
- Wrote master state files: `sprint-tracker.md`, `agents-playbook.md`, `skills-roadmap.md`, `session-notes/README.md`
- Wrote 16 ADRs (ADR-001 through ADR-016) capturing every locked brainstorming decision
- Wrote detailed Sprint 1 plan + skeletons for Sprints 2-6
- Bootstrapped `hadouta-backend`: spec-kit + Hono + Drizzle + Better-Auth + AI SDK scaffolding, project-scope `.claude/settings.json`, custom constitution, initial routes (`/health`, `/waitlist`)
- Bootstrapped `hadouta-web`: Next.js 16 + React 19 + Tailwind 4 + shadcn/ui (RTL enabled) + spec-kit, full Arabic landing page with WaitlistForm component, custom constitution, project-scope `.claude/settings.json`
- Both repos: initial commits made

---

## Decisions made (all captured as ADRs)

- ADR-001 — Real business intent (not learning project)
- ADR-002 — Egyptian cultural specificity is the moat
- ADR-003 — MVP anchored to First Day of School, Sept 2026
- ADR-004 — Digital-first MVP, print upgrade in v1.5
- ADR-005 — L3 photo upload + watercolor style (NOT photoreal 3D)
- ADR-006 — AI stack: Claude Sonnet 4.6 + Haiku 4.5 + Nano Banana 2 (digital) / Pro (print 4K) / GPT Image 2 (cover fallback)
- ADR-007 — Frontend: Next.js + shadcn/ui (NOT Angular)
- ADR-008 — Backend: Node + Hono + TypeScript (NOT .NET)
- ADR-009 — Database: Neon + Better-Auth + Cloudflare R2 (NOT Supabase)
- ADR-010 — Trigger.dev v3 with waitpoints
- ADR-011 — Two repos with OpenAPI for type sync
- ADR-012 — Layered validator architecture (Universal + Theme-specific)
- ADR-013 — Active learning loop with manual approval gate
- ADR-014 — Pricing: A/B test 250 vs 300 EGP digital (TENTATIVE)
- ADR-015 — Validation parallel with build (Lean Startup)
- ADR-016 — Distribution: Facebook + influencers (phased) + organic mom groups

### Open / tentative decisions
- Final pricing: locked after Sprint 1 ad campaign data
- Cairo print partner: research in Sprint 1 Track B
- Bosta merchant rates: confirmed in Sprint 1 Track B
- Brand handles: register `@hadouta` on Instagram/TikTok/Facebook in Sprint 1

---

## Code/files changed

### Created
- `/home/ahmed/Desktop/hadouta/docs/` — entire shared docs structure (sprints, decisions, session-notes, agents-playbook, skills-roadmap)
- `/home/ahmed/Desktop/hadouta/hadouta-backend/` — full backend bootstrap (Hono + Drizzle + Better-Auth + AI SDK + spec-kit)
- `/home/ahmed/Desktop/hadouta/hadouta-web/` — full frontend bootstrap (Next.js + Tailwind 4 + shadcn/ui + RTL Arabic + spec-kit)

### User-scope settings updated this session (in `~/.claude/settings.json`)
- Added comprehensive permissions allowlist (file ops, web fetch, package managers, git, gh, docker, etc.)
- Added small deny list for catastrophic patterns (rm -rf /, etc.)
- Cleaned `~/.claude/settings.local.json` (removed obsolete one-off entries; **rotated note**: an admin API key was found embedded in old entries — Ahmed needs to rotate that key on the property-agent-backend service)
- Pruned `~/.claude/backups/` (kept newest), `~/.claude/paste-cache/` (cleared), `~/.claude/shell-snapshots/` (kept newest)

---

## Agents delegated to

This session was almost entirely manager (Claude) direct work — appropriate for project bootstrap. No specialist agents delegated. Agent delegation begins in Sprint 2-3 when AI pipeline complexity arrives.

Research forks launched (worth noting since user later asked for token efficiency):
1. Market research fork — competitive landscape (Qessati, magicalchildrensbook, Wonderbly, etc.)
2. Cost & pricing research fork — unit economics + AI model comparison
3. Stack research fork — Hono + Next.js + Trigger.dev recommendations
4. Image-model + Supabase alternatives fork — Nano Banana 2 vs GPT Image 2; Neon vs Supabase

After fork #4, user asked for no more forks. Honored.

---

## Tests / verification

- ✅ `uv --version` returns 0.11.8
- ✅ Backend `pnpm install` would work (deps spec'd, no install yet — no `node_modules` because user hasn't asked to verify dev server boot)
- ✅ Frontend `pnpm install` ran during `create-next-app` and `shadcn add`
- ✅ Both repo first commits succeeded
- ⚠️ **NOT VERIFIED**: dev servers booting locally (user hasn't asked, no `.env` files filled with real DB URL etc.)
- ⚠️ **NOT VERIFIED**: backend `tsc --noEmit` clean (the Drizzle deps + Hono types haven't been installed)

---

## Open issues to address next session

1. **Install backend node_modules** + verify `pnpm dev` boots Hono on port 3001 with `/health` returning 200
2. **Install frontend deps** (already done by create-next-app and shadcn) but verify `pnpm dev` boots and `/` renders Arabic landing page
3. **Connect them**: with backend running, submit waitlist form from frontend; verify CORS works and console shows the signup data
4. **Set up Neon project** (Ahmed): create free-tier project, get DATABASE_URL, run first Drizzle migration
5. **Set up Better-Auth** properly: configure session storage in Drizzle, wire to Hono routes
6. **Brand assets**: domain registration + trademark search + social handles (Sprint 1 Track B Day 1-2 work)

---

## Where to resume

> **Open `docs/sprints/sprint-tracker.md`. It will show "Sprint 0 → done; Sprint 1 ready to start."**
>
> **Then open `docs/sprints/sprint-01-foundation.md` and start with Track A1: implement `/health` route in backend** (already exists from this session's bootstrap — verify it boots) **then move to Track A2: ensure waitlist endpoint persists to Neon (currently logs to console as a placeholder).**
>
> **In parallel, Ahmed begins Track B Day 1-2 tasks**: domain registration, trademark search, handle reservation, service signups.

---

## Notes / observations (for skills-roadmap.md tracking)

- **Pattern noticed**: writing 16 ADRs in succession had a clear template. After 5+ more ADRs are written in future sessions, this is a strong candidate for the `write-adr` skill (skills-roadmap.md item #5).
- **Pattern noticed**: writing the session note + updating sprint-tracker + committing — this is exactly the `wrap-up-hadouta-session` candidate (skills-roadmap.md item #2). Codify after 3 sessions.
- **Friction**: `pnpm dlx shadcn@latest init` flag names changed from earlier guides (`--base-color` no longer exists; replaced by `--base`). Document this in skills-roadmap if shadcn add becomes a recurring task.
- **Friction**: The umbrella `/home/ahmed/Desktop/hadouta/` directory is not yet a git repo. The shared docs/ are not version-controlled. This is a small risk — fix early next session by `git init`'ing the umbrella with `.gitignore` excluding the sub-repos.
- **Cost note**: today's session used 4 research forks + significant token volume. User asked for tighter token use mid-session; honored from that point forward. Future sessions should default to inline research unless explicitly worth a fork.
- **Decision velocity**: Ahmed answered most brainstorming questions decisively after seeing the option matrix. Future sessions can move faster on similar decision-tree work.

---

**Session ended cleanly. Both repos committed. Ready for Sprint 1.**

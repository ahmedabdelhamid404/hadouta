# The umbrella folder is its own git repo for SHARED docs only.
# The two sub-repos (hadouta-backend, hadouta-web) are independent git repos
# and must NOT be tracked here — they have their own histories.

hadouta-backend/
hadouta-web/

# OS / temp
.DS_Store
*.swp
*.tmp

# Editor
.vscode/
.idea/

# Claude Code session-local artifacts (personal allowlist, not shared)
.claude/settings.local.json

# Playwright MCP runtime artifacts
.playwright-mcp/

# Third-party reference screenshots captured during research (copyright not ours; reference only)
magicalchildrensbook-landing.png
*-reference.png

# Operations credentials (.env.local holds Sentry + PostHog tokens for runbook scripts)
.env.local
.env

# Visual-companion brainstorming session artifacts (mockup HTML + state)
.superpowers/
